const INITIAL_STATE = {
    products: []
};

const shopReducer = (state = INITIAL_STATE, action) => {
    // switch (action.type) {
    //     case "test":
    //         return {
    //             ...state,
    //             products: [
    //                 {
    //                     a: 1
    //                 }
    //             ]
    //         }
    //     default:
    //         return state;
    // }

    return state;
};

export default shopReducer;